/*  Author: Mateus Henrique
    Purpose: This program shows functions from "math.h" header file.
*/

/*
    Math functions (<math.h>)
    acosh() - Retunrs the hyperbolic arccosine of x.
    asin() - Returns the arcsine of x as an angle.
    atan() - Returns the arctangent as an angle.
    cos() - Returns the consine of x.
    sin() - Returns the consine of x.
    tan() - Returns the tangent of x.
    ceil() - Rounds x to the closest biggest integer value.
    floor() - Rounds x to the closest smallest integer value.
    log() - Returns the natural log of x.
    nan() - returns a NaN. Tells if it's a number.
    pow() - Returns x raised to some power.
    sqrt() - Returns the square root of x.
    remainder() - Returns the remainder of x divided by y.
    round() - Rounds to the nearest integer.
*/

#include <stdio.h>

int main(void)
{
    return 0;
}